<div class="flex gap-2">
                                <a href="{% url 'documents:verification_dashboard' %}" class="px-3 py-1 bg-blue-600 text-white rounded text-sm hover:bg-blue-700 transition-colors">
                                    <i class="fas fa-eye mr-1"></i>Review Documents
                                </a>
                                {% if user.role == 'loan_officer' %}
                                <form method="POST" action="{% url 'documents:approve_client_documents' verification.client.id %}" style="display: inline;">
                                    {% csrf_token %}
                                    <button type="submit" class="px-3 py-1 bg-green-600 text-white rounded text-sm hover:bg-green-700 transition-colors">
                                        <i class="fas fa-check mr-1"></i>Approve All
                                    </button>
                                </form>
                                <form method="POST" action="{% url 'documents:reject_client_documents' verification.client.id %}" style="display: inline;">
                                    {% csrf_token %}
                                    <button type="submit" class="px-3 py-1 bg-red-600 text-white rounded text-sm hover:bg-red-700 transition-colors">
                                        <i class="fas fa-times mr-1"></i>Reject All
                                    </button>
                                </form>
                                {% elif user.role == 'manager' %}
                                <a href="{% url 'documents:verification_dashboard' %}" class="px-3 py-1 bg-purple-600 text-white rounded text-sm hover:bg-purple-700 transition-colors">
                                    <i class="fas fa-eye mr-1"></i>View Details
                                </a>
                                {% endif %}
                            </div>
